import './App.css'

function App() {

  const ofThree = [0,1,2]

    return (
      <>
        {
          ofThree.map((i) => 
          (
            <div className="board-row">
              {ofThree.map((j) => (<button className="square">X</button>))}
            </div>
          ))
        }
      </>
    )
}

export default App
